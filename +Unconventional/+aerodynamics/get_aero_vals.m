function [aerovals] = get_aero_vals(obj,alt,reynolds)
%GET_AERO_VALS 
% NOTE: Currently just for cruise configuration

% coefficient of lift from AVL - requires lookup table NOTE only for low
% angles of attack
CL = ;
% induced drag from AVL - requires lookup table
aerovals.CD = ; 


end